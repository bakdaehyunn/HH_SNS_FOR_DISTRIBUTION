<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.spring.ex06.ReplyMapper">
	<insert id="insert">
		insert into ${reply}
		(${replyId}, ${feedId}, ${userId}, ${userNickname}, ${replyContent} )
		values (reply_seq.nextval, #{feedId}, #{userId}, #{userNickname}, #{replyContent})
	</insert>
	
	<select id="select_all_by_feed_id" resultType="ReplyVO">
		select * from ${reply}
		where ${feedId} = #{feedId}
		order by ${replyId}
	</select>
	
	<update id="update">
		update ${reply}
		set ${replyContent} = #{replyContent}, ${replyDate} = sysdate
		where ${replyId} = #{replyId}
	</update>
	
	<delete id="delete">
		delete from ${reply}
		where ${replyId} = #{replyId}
	</delete>
</mapper>